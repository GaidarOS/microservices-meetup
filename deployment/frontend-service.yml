apiVersion: v1
kind: Service
metadata:
  labels:
    run: frontend
  name: frontend
  namespace: default
spec:
  ports:
  - nodePort: 30080
    port: 4200
    targetPort: 4200
  selector:
    run: frontend
  sessionAffinity: ClientIP
  type: NodePort
